---
# Minimal example: Just a router and a server
all:
  children:
    network:
      hosts:
        er8411:
          ansible_host: 192.168.0.1
          device_type: router
          description: "TP-Link ER8411 Router"
    infrastructure:
      hosts:
        truenas:
          ansible_host: 192.168.0.10
          role: storage
          platform: truenas
          description: "TrueNAS storage server"
